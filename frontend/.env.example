# Dev only: where `vite dev` proxies /api and /uploads. Defaults to
# http://localhost:8080. Production needs no API URL — nginx proxies /api to
# the backend on the same origin (set BACKEND_URL on the frontend container).
VITE_DEV_API_TARGET=http://localhost:8080
